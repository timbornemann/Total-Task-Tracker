import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.total_task_tracker',
  appName: 'Total Task Tracker',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
